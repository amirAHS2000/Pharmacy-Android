package com.example.pharmacyapp.ui.prepare

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PrepareViewModel(application: Application) : AndroidViewModel(application) {
}